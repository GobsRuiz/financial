{
  "name": "financeiro",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run mock\" \"npm run client\"",
    "mock": "json-server --watch db.json --port 3001 --delay 100",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "concurrently": "^9.0.0",
    "json-server": "^0.17.4"
  }
}
